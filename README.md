# EasyScoreboard

Paper & Spigot — Kotlin — scoreboard.

Easy `config.yml` boards (title, lines, permission, animation), DB `scoreboard_prefs`, PlaceholderAPI.

```bash
gradle shadowJar
# → build/libs/easyscoreboard-1.0.0.jar
```
